{
  "name": "kaleidoGL",
  "version": "0.0.1",
  "devDependencies": {
    "browserify": "latest",
    "watchify": "latest",
    "browserify-shader": "latest"
  },
  "scripts": {
    "compile": "browserify -t browserify-shader src/kaleidoGL.js -o kaleidoGL.js",
    "watch": "watchify -t browserify-shader src/kaleidoGL.js -o kaleidoGL.js -v"
  },
  "dependencies": {
    "browserify-shader": "^0.1.2"
  }
}
